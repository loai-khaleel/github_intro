# github_intro

the is first lecture in gethub services

### project setup

#### setup view

run the following command on , cmd,powershell,gitbash

```
python -m venv myvenv
```

```
myvenv/scripts/activate/
```

run the following command to install the package

```
pip install pkgName
```
